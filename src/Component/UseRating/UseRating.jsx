import React from 'react';

const UseRating = (props) => {

    const {rating} = props.rating;

    return (
        <>
            if(rating == 5){

            }
        </>
    );
};

export default UseRating;